export class popup {
  constructor({ popupSelector }) {
    this.popupElement = document.querySelector(popupSelector);
  }

  open() {
    //opens popup
  }

  close() {
    //close popup
  }
  _handleEScClose() {
    //listens for esp button
  }
  setEventListeners() {
    //sets evt listeners
  }
}
